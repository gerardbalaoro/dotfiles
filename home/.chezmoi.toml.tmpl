{{- $presets := promptMultichoice
    "Choose presets to apply to this machine"
    (list "dev" "gaming")
    (list)
-}}

[data]
    presets = {{- $presets | toToml -}}

{{- if eq .chezmoi.os "linux" }}
{{- if (.chezmoi.kernel.osrelease | lower | contains "microsoft") }}
[onepassword]
    command = "op.exe"
{{- end }}
{{- end }}
