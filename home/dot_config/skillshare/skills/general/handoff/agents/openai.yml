interface:
  display_name: "Handoff"
  short_description: "Prepare context for another agent session"
  default_prompt: "Use $handoff to prepare this conversation for another agent to continue."

policy:
  allow_implicit_invocation: false
